This program allow user to run any command on all devices in DNAC. It first list all devices, then run "show run" command
You need to know what commands are not supported on specific platform.
For example, AP does not support show run. I exclude AP from show run
